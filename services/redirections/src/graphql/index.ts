import { GET_REDIRECT, GET_REDIRECT_META } from "./queries";
import { CREATE_REDIRECT } from "./mutations";

export { createClient } from "./client";

export const QUERIES = {
  GET_REDIRECT,
  GET_REDIRECT_META
};

export const MUTATIONS = {
  CREATE_REDIRECT
};
